<!DOCTYPE html>
<html lang="en" class="h-full bg-zinc-950">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title ? `${title} · ` : '' %>AdaptLearn AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-full bg-zinc-950 text-zinc-200">
    <header class="border-b border-zinc-800 bg-zinc-950/70 backdrop-blur">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <a href="/" class="text-sm font-semibold tracking-wide text-zinc-100">AdaptLearn AI</a>
        <nav class="flex items-center gap-3 text-sm">
          <% if (user) { %>
            <a href="/dashboard" class="text-zinc-300 hover:text-zinc-100">Dashboard</a>
            <form method="POST" action="/logout">
              <button type="submit" class="rounded-md border border-zinc-800 bg-zinc-950 px-3 py-1.5 text-zinc-200 hover:bg-zinc-900">Logout</button>
            </form>
          <% } else { %>
            <a href="/login" class="text-zinc-300 hover:text-zinc-100">Login</a>
            <a href="/register" class="rounded-md bg-indigo-600 px-3 py-1.5 text-white hover:bg-indigo-500">Create account</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main>
      <%- body %>
    </main>
  </body>
</html>
